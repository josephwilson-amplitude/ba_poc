import { FlagConfigCache, FlagConfig } from '../types/flag';
export declare class InMemoryFlagConfigCache implements FlagConfigCache {
    private readonly store;
    private cache;
    constructor(store?: FlagConfigCache, flagConfigs?: Record<string, FlagConfig>);
    getAllCached(): Record<string, FlagConfig>;
    get(flagKey: string): Promise<FlagConfig>;
    getAll(): Promise<Record<string, FlagConfig>>;
    put(flagKey: string, flagConfig: FlagConfig): Promise<void>;
    putAll(flagConfigs: Record<string, FlagConfig>): Promise<void>;
    delete(flagKey: string): Promise<void>;
    clear(): Promise<void>;
}
