"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Assignment = void 0;
const string_1 = require("../util/string");
/**
 * @deprecated Assignment tracking is deprecated. Use Exposure tracking.
 * Making this class a synonym for Exposure. They have the same fields.
 * This class can be removed in the future with little effort.
 */
class Assignment {
    constructor(user, results) {
        this.timestamp = Date.now();
        this.user = user;
        this.results = results;
    }
    canonicalize() {
        let canonical = `${(0, string_1.safeStringTrim)(this.user.user_id)} ${(0, string_1.safeStringTrim)(this.user.device_id)} `;
        for (const key of Object.keys(this.results).sort()) {
            const variant = this.results[key];
            if (variant === null || variant === void 0 ? void 0 : variant.key) {
                canonical +=
                    (0, string_1.safeStringTrim)(key) + ' ' + (0, string_1.safeStringTrim)(variant.key) + ' ';
            }
        }
        return canonical;
    }
}
exports.Assignment = Assignment;
