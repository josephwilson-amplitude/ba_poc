import { LocalEvaluationClient } from './local/client';
import { ExperimentClient, RemoteEvaluationClient } from './remote/client';
import { ExperimentConfig, RemoteEvaluationConfig, LocalEvaluationConfig } from './types/config';
/**
 * Provides factory methods for storing singleton instances of
 * {@link ExperimentClient}.
 *
 * @category Core Usage
 */
export declare const Experiment: {
    initialize: (apiKey: string, config?: ExperimentConfig) => ExperimentClient;
    initializeRemote: (apiKey: string, config?: RemoteEvaluationConfig) => RemoteEvaluationClient;
    initializeLocal: (apiKey: string, config?: LocalEvaluationConfig) => LocalEvaluationClient;
};
