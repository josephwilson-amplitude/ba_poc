type BackoffPolicy = {
    attempts: number;
    min: number;
    max: number;
    scalar: number;
};
declare function doWithBackoff<Result>(action: () => Promise<Result>, backoffPolicy: BackoffPolicy): Promise<Result>;
declare function doWithBackoffFailLoudly<Result>(action: () => Promise<Result>, backoffPolicy: BackoffPolicy): Promise<Result>;
export { doWithBackoff, doWithBackoffFailLoudly, BackoffPolicy };
