import { Exposure, ExposureFilter } from './exposure';
export declare class InMemoryExposureFilter implements ExposureFilter {
    ttlMillis: number;
    private readonly cache;
    constructor(size: number, ttlMillis?: number);
    shouldTrack(exposure: Exposure): boolean;
}
