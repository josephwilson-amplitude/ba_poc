import { BaseEvent, CoreClient } from '@amplitude/analytics-types';
import { Exposure, ExposureFilter, ExposureService } from './exposure';
export declare const DAY_MILLIS: number;
export declare const FLAG_TYPE_MUTUAL_EXCLUSION_GROUP = "mutual-exclusion-group";
export declare class AmplitudeExposureService implements ExposureService {
    private readonly amplitude;
    private readonly exposureFilter;
    constructor(amplitude: CoreClient, exposureFilter: ExposureFilter);
    track(exposure: Exposure): Promise<void>;
}
export declare const toExposureEvents: (exposure: Exposure, ttlMillis: number) => BaseEvent[];
